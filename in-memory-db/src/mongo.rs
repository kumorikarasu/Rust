//use database_traits::Database;

/*
pub struct Mongo<T> {
    pub connection: String,
}

impl Database<T> for Mongo<T> {
    fn create(&self, entity: T) -> T {
        entity
    }

    fn read(&self, id: i32) -> T {
        entity
    }

    fn update(&self, entity: T) -> T {
        entity
    }

    fn delete(&self, id: i32) -> T {
        entity
    }
}
*/
