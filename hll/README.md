# HyperLogLog Event Aggregator

Compact HyperLogLog implementation wired into an in-memory event aggregator; ingests millions of synthetic events, tracks per-game/per-event counts, and estimates unique timestamps using hash-based sketches. Shows comfort with probabilistic data structures, streaming aggregation, and low-level hashing choices.
